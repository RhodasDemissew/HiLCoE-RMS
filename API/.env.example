# Rename this file to .env and set your values
MONGO_URI=mongodb://localhost:27017/hilcoe_rms
# For MongoDB Atlas, use the SRV connection string (uncomment and fill values):
# MONGO_URI=mongodb+srv://<USER>:<PASSWORD>@<cluster>.mongodb.net/hilcoe_rms?retryWrites=true&w=majority
# Notes:
# - Create a Database User in Atlas with readWrite on hilcoe_rms
# - Add your IP to Atlas Network Access (or use 0.0.0.0/0 temporarily for testing)
# - If password has special characters, URL-encode them (e.g., @ => %40)
PORT=4000
JWT_SECRET=dev_secret_change_me
STORAGE_DIR=./storage
